name: cdm_streamguard
version: '1.0.0'
config-version: 2

profile: cdm_dremio

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  cdm_streamguard:
    bronze:
      +materialized: incremental
      +schema: bronze
    silver:
      +materialized: incremental
      +schema: silver
    gold:
      +materialized: table
      +schema: gold
